{
  "app-type": "<% if (external.isApp) { %>app<% } else { %>module<% } %>",
  "modules": {
    <% if (!external.isApp) { %><% if (theme==='platform-admin') { %>"@wt/audit-logging-platform-admin": "latest",<% } %><% } %>
    "@wt/membership-share": "latest"
  },
  "theme": "<%= theme %>"
}
